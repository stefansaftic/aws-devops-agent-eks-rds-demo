# Logs from experiment runs
*.log

# Terraform state
*.tfstate
*.tfstate.backup
.terraform/

# OS files
.DS_Store

# Claude Code per-session permission allowlist may capture full bash
# command lines, including any export AWS_SESSION_TOKEN=... lines run
# in the shell. Never commit it.
.claude/settings.local.json
